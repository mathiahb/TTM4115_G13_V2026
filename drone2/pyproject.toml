[project]
name = "drone2"
version = "0.1.0"
description = "Refactored drone delivery client"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0",
    "paho-mqtt>=2.1.0",
    "stmpy>=0.7.0",
]
