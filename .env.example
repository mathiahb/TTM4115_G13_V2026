# Server host IP (used by drone compose to reach the remote broker)
SERVER_HOST=192.168.1.100

# Drone ID (1, 2, 3, ...) - must match an entry in the server's config.yaml
DRONE_ID=1

# Flask secret key (server only)
FLASK_SECRET=change-me-in-production
